<!DOCTYPE html>
<html>
   <head>
      <title>TheToolboxStore</title>
      <%= csrf_meta_tags %>
      
      <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
      <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
   </head>
   <body>
      <nav class="navbar navbar-expand" style="background-color: #020f18;">
         <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
         <span class="navbar-toggler-icon"></span>
         </button>
         <a class="navbar-brand" href="#">The Toolbox Store</a>
         <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
               <li class="nav-item active">
                  <a class="nav-link" href=<%= root_path %>>Home<span class="sr-only">(current)</span></a>
               </li>
               <li class="nav-item">
                  <a class="nav-link" href=<%= sale_path %>>Sale</a>
               </li>
               <li class="nav-item">
                  <a class="nav-link" href=<%= new_path %>>New</a>
               </li>
               <li class="nav-item">
                  <a class="nav-link" href="#">About</a>
               </li>
            </ul>
            </div>
            <%= form_tag(search_path, :method => "get", id: "search-form", class:"form-inline my-2 my-lg-0") do %>
                <%= select_tag :filter, options_from_collection_for_select(@category_list, 'id', 'name', 0), class: "form-control mr-sm-2", :include_blank => 'Shop by Category' %>
                <%= text_field_tag(:search, params[:search], placeholder: "Search Products", class: "form-control mr-sm-2") %>
                <%= submit_tag("Search", class: "btn btn-outline-success my-2 my-sm-0") %>
            <% end %>
                <%= form_tag(cart_path, :method => "get", class:"form-inline my-2 my-lg-0") do %>
                <%= submit_tag("Cart", class: "btn btn-outline-success my-2 my-sm-0") %>
            <% end %>
         </div>
      </nav>

      <% if flash[:notice]%>
        <div class="alert alert-info" role="alert">
           <%= flash[:notice] %> 
        </div>
      <% end %>

      <div class="container">
         <%= yield %>
      </div>
   </body>
</html>